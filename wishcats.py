def mix_members(members):
    # answer = {}
    # if len(members) % 7 == 0:
    #     for i in members:
    #         answer[i] = members[i]
    # if len(members) % 6 == 0:
    #     for i in members:
    #         answer[i] = members[i]
    # if len(members) % 5 == 0:
    #     for i in members:
    #         answer[i] = members[i]
    print(members)
    return members

mix_members(["김수한무","거북이","두루미","배트맨","슈퍼맨","엑스맨","젠틀맨"])